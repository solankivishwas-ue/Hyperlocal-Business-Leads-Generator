# Core
Django>=4.2,<5.0
pandas>=2.0.0
requests>=2.31.0

# Email enrichment (P1)
# Hunter.io is accessed via plain requests — no extra client package needed.
# beautifulsoup4 is used for website scraping fallback.
beautifulsoup4>=4.12.0

# Env loading (no python-dotenv needed — main.py does it natively)
# python-dotenv>=1.0.0   # optional: uncomment if you prefer dotenv

# Dev / testing
# pytest>=8.0
# pytest-django>=4.8
